<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        INTERNET is required for:
          - HTTP transport (OkHttp to 127.0.0.1)
          - gRPC transport (grpc-java client setup, even over UDS, uses network stack)
        Loopback (127.0.0.1) and UDS do NOT require LOCAL_NETWORK permission on Android.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DotnetNativeInteropApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.DotnetNativeInterop"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
